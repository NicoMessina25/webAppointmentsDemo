export default function RadioButtonGroup({options, value, setValue, header}:any){
    return(
        <div>
            <p>{header}</p>
            {options.map((op:any, ind:number)=>{

            })}
        </div>
    );
}